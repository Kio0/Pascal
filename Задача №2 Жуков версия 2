var n,m: integer; //n-количество субьектов. m-количетсво объектов.
var a: array[1..100] of array[1..100] of integer; //сперва объекты затем субъекты.
var LO,LS: array[1..100] of string; //массив уровней доступа и уровней секретности
//Объект может иметь право на чтение (если его ранг выше) иметь право на запись(если его ранг равен) и не иметь прав (если его ранг ниже)
 
var Max: integer; //максимальный ранг на данный момент

//алгоритм работы: 
//1)Всем субьектам, которые не могут читать и писать в файлы, даём право 1
//2)Всем объектам, которые могут писать или читать все субьекты, даём право 1
//3)Ищем любого субьекта, который может только писать файлы, но не читать. Даём ему и всем файлам, которые он пишет, ранг 2.
//Во всех вышеуказанных ситуациях объекты и субьекты с уже присвоенным рангом не рассматриваются.
//повторяем пока таблица не закончится для рангов 3-4, 5-6 и.т.д. 
//Если все субьекты могут читать какие-либо файлы, но нет файла, который могут писать или читать все субьекты, задача не имеет решения. (так как не существует минимального ранга)
//Если после распределения рангов возникают противоречия, задача не имеет решения.

Function StatusOb(ID: integer):integer; //по ID объекта возвращает числа от 0 до 1. 0 - объект могут писать либо читать все субьекты. 1 - есть субъект, не имеющий никаких прав над объектом
var i,r: integer;
begin
  r:=0;
  for i:=1 to n do if a[ID,i]=0 then r:=1;
  StatusOb:=r;
end;
Function StatusS(ID: integer):integer; 
//по ID субьекта возвращает числа от 0 до 1. 
//0 - субьект не имеет прав ни на один объект. 
//1 - сущесвует объект, который этот субъект может писать, но не существует объекта, который субьект может читать. 
//2 - сущесвует объект, который этот субъект может читать
begin
  
  
end;

Procedure GiveRang(ID,n: integer); //даёт субьекту ID ранг n если ID>0 и даёт объекту -ID ранг n если ID < 0
begin
  
end;

Procedure GiveS();//Всем субьектам, которые не могут читать и писать в файлы, даём право 1
begin
 
  
end;
Procedure GiveO();//Всем объектам, которые могут писать или читать все субьекты, даём право 1
begin
  
  
end;
Procedure Give2();
begin
  
end;

Function GetTable(): boolean; //создаёт таблицу с мандатной политикой доступа. Возвращает false если это невозможно
var i,r: integer;
begin
GiveS;
GiveO;
Give2;
  
  
end;

Procedure ReadFile(); //считывает матрицу N*M из файла file.txt В каждой строке один субьект. Символы строк - буквы n,r,w. n-нет никаких прав. r-только чтение. w-и чтение и запись.
var i,r: integer;
var s:string;
var t: text;
begin


assign(t,'file.txt');
reset(t);
while not(eof(t)) do begin
  n:=n+1;
  readln(t,s);
  m:=length(s);
  for i:=1 to m do begin 
  if s[i]='n' then a[i,n]:=0; //нет никаких прав
  if s[i]='r' then a[i,n]:=1;
  if s[i]='w' then a[i,n]:=2;
  end;

end;

close(t);
end;
begin
ReadFile();
writeln(GetTable);
writeln(n);

end.
